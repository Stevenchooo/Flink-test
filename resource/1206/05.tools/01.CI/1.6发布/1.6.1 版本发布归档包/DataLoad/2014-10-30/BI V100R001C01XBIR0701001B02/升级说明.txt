（*1）       当前没法查看入库的文件有哪些？之前可以通过TCC上的批次运行状态，查看文件源信息,来检查入库文件的数量和文件名称；
        ---- 控制台日志显示出每个入库的文件信息，成功的文件和失败的文件 
           * 增加文件统计日志		

（*2）       推送字段个数和类型不一致的时候不会报错，导致入库有问题；
        ---- 检查前100行，匹配字段个数是否正确；
           * 增加以下两个配置属性
             <!-- 文件列数 <=0表示不校验 -->
			 <FileColumns>2</FileColumns>
			 <!-- 文件字段的分割符，默认使用\0x01 -->
			 <Separator>,</Separator>		   

（*3）       文件源被要求是文本文件，如果是已经压缩的，这样用新工具入库是有问题的
        ---- 至少支持一种压缩格式（lzo,gz），如果业务传输必须压缩，约定为当前工具支持的压缩格式
           * 增加支持lzo,gz,bz压缩格式的文件

（*4）   新工具配置文件中的等待时间和文件个数是“或”的关系，对于一些死等任务的，没法满足条件。
        ---- 需要支持：文件个数满足后，就开始执行入库；文件一直未满足，但到等待时间后，无论文件多少也都正常入库；
           * 增加配置属性
             <!-- 超过等待时间是否检查文件个数，默认true -->
			 <TimeOutCheckFile>true</TimeOutCheckFile>		
m>1	n>1				
文件个数	等待时间	超时后检测个数	结束条件	出错	成功
-1	n	-	等待n分钟	文件入库0个出错	文件入库>=1个成功
m	-1	-	无	文件入库<m个报错	文件入库>=m个成功
m	n	TRUE	"文件到达m个
或者等待n分钟"	文件入库<m个出错	文件入库>=m个成功
m	n	FALSE	"文件到达m个
或者等待n分钟"	文件入库0个出错	文件入库>=m个成功
			 

（*5）   新工具的只支持时间分区；
        ---- 需要扩展：按文件名中的中一段进行分区
           * 增加非时间分区的支持，文件和分区对应关系举例：
		     文件名：DATA_23_IM_201407010000000_1.log
		             <Partition>pt_d,version_code,devip</Partition>
					 时间之前用"_"分割的字符串对应分区值，version_code=23， devip=IM
					 
（*6）    入库文件添加空文件标志
           * 增加是否入空文件标示
             <!-- 空文件是否入库 默认true -->
			 <DumpFileLoad>true</DumpFileLoad>		   