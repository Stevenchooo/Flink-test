功能特性：
导入HDFS:
1. 做字段列数检查时，采用最大匹配规则，例如：”1,2,3,”按”,”分割后是4列；
2. 分区名按下划线分割，通过配置选择某段为分区；
3. 增加配置属性和命令行参数fileskip和fileskiptime，为true时库中已经存在的文件不重复入库，默认关闭；
4. 日志中增加入库文件大小和入库时长的统计；
5. 增加单文件入库重试机制，最多尝试3次。

导出MYSQL(旧工具)：
1. hivetomysql的任务，当hive结果为空时，也需要推送到mysql中；
2. 优化导出到mysql和文件的内存使用，支撑千万记录入库。
