版本变动内容：
TCC后台：
   1、TCC支持存储任务与hive表的关系
   2、TCC任务实例结束通知数据质量监控系统
   3、TCC登陆页面增加验证码功能
   4、TCC的增加密码复杂度校验功能
   5、其它fortify工具检查的安全问题修改
   6、修复任务周期详情页面的依赖周期、批次状态、步骤状态记录过多时，页面无法正常显示问题；

升级：
	alter table `tcc_task` add `hive_table_name` varchar(1024) DEFAULT NULL;